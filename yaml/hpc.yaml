# a slurm and apptainer template for our hpc named cluster
- scope: process
  name: "demo_p1"
  manager: "slurm"
  manager_slurm: { "-p":"cluster,dedicated"}
  environment: "apptainer"
  environment_apptainer: { "-B":"/nexus:/nexus" }
  automated_mount: False
  # before_script: "module load apptainer"
  # manager_slurm: {"--ntasks-per-core":"2", "-p":"general,small" }

- scope: process
  name: "demo_p2"
  manager: "slurm"
  manager_slurm: { "-p":"cluster,dedicated" }
  environment: "apptainer"
  environment_apptainer: { "-B":"/nexus:/nexus" }
  automated_mount: False
  # before_script: "module load apptainer"
  # manager_slurm: {"--ntasks-per-core":"2", "-p":"general,small" }

- scope: process
  name: "demo_p3"
  manager: "slurm"
  manager_slurm: { "-p":"cluster,dedicated"}
  environment: "apptainer"
  environment_apptainer: { "-B":"/nexus:/nexus" }
  automated_mount: False
  # before_script: "module load apptainer"
  # manager_slurm: { "--ntasks-per-core":"2", "-p":"general,small" }

- includes: 
    - nexus.apptainer.yaml
    
# - scope: global
#   manager: "slurm"
#   environment: "apptainer"

